{
    "name": "rspack-express-jwt",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "start": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
    },
    "devDependencies": {
        "concurrently": "^9.0.0"
    }
}
